import MobileNavBarMenuModal from './MobileNavBarMenuModal'

export const generated = (args) => {
  return <MobileNavBarMenuModal {...args} />
}

export default { title: 'Components/MobileNavBarMenuModal' }
